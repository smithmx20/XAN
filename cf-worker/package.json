{
  "name": "xan-stream-proxy",
  "version": "6.0.0",
  "private": true,
  "description": "XAN Cloudflare Worker — stream proxy + AllAnime episode resolver with direct crypto + Browser Rendering fallback for __aaCrypto bootstrap",
  "main": "worker.js",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "tail": "wrangler tail"
  },
  "dependencies": {
    "@cloudflare/puppeteer": "^0.0.14"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
